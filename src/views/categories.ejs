<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <h2>See the Categories</h2>
    <ul class="category-list">
    <% categories.forEach(category => { %>
        <li><%= category.name %></li>
    <% }); %>
    </ul>
</main>
<%- include('partials/footer') %>